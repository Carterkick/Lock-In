{
  "name": "lock-in-sync-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker: push reminders and cloud backup for the Lock In PWA.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@block65/webcrypto-web-push": "2.0.0"
  },
  "devDependencies": {
    "wrangler": "4.131.1"
  }
}
